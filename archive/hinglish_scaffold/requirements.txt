# Core (harness, tests, mock backend) — CPU only
numpy>=1.26
pyyaml>=6.0
jiwer>=3.0
indic-transliteration>=2.3   # Devanagari <-> Roman for script-agnostic scoring
pytest>=8.0

# Audio I/O
soundfile>=0.12
librosa>=0.10

# Real backends — install on the GPU runtime only
# torch>=2.3
# transformers>=4.44
# accelerate>=0.33
# datasets>=2.20          # IndicVoices loader
# peft>=0.12              # phase 2
